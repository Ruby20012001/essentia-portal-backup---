import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function ClientPortalPage() {
  return (
    <PagePlaceholder
      title="Client Portal"
      section="S15 · Phase 3 · The family's window into their project — Blueprint §04"
    />
  );
}
